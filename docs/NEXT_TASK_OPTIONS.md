# Next Task Options

The workspace is now ready for the next concrete M6 generation task. The strongest evidence-backed options are:

1. Generate the backend solution scaffold for `platform-app`.
2. Generate preserved API contract specs and contract tests for Wave 0 endpoints.
3. Generate the React route map and frontend feature skeleton.
4. Generate the PostgreSQL ownership and migration blueprint.
5. Generate Docker, Kubernetes, and Terraform skeletons for the modular-monolith-first deployment.

If the next task is not specified, the safest next implementation step is option `2`, because preserved contract tests reduce migration risk before deeper code generation.
